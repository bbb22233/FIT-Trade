# P1-007 — Phase 1 integration, recovery, and security exit review

## Identity

- Task ID: `P1-007`
- Owner: local Codex coordinator
- Branch: `codex/p1-exit-review`
- Worktree: `/Users/guanlan/Documents/FIT-Trade-worktrees/p1-exit-review`
- Base: exact accepted P1-006 commit, which must descend linearly from every
  accepted P1-001 through P1-005 commit

## Objective

Prove Phase 1 interoperability, transaction atomicity, identity isolation,
idempotent delivery, crash recovery, secret hygiene, and default-disabled
infrastructure without reassembling or repairing the accepted lineage.

## Owned paths

- Allowed: `coordination/evidence/P1-007/**`
- Read-only: every other repository path
- Forbidden: contract, application, infrastructure, migration, and task-packet
  changes

## Deliverables

- Integration manifest with exact ancestry, patch IDs, component tree IDs, and
  changed-path ownership
- Machine-readable evidence index for every Phase 1 acceptance check
- Frozen workload inputs, command transcripts, artifact hashes, timestamps,
  versions, and observed RPO/RTO report
- Independent server Codex and local Hermes review handoffs

## Review roles

- Local Codex records evidence and coordinates the fixed candidate.
- Server Codex uses a detached read-only worktree for Linux, PostgreSQL, NATS,
  process-kill, egress, and recovery probes.
- Local Hermes uses a read-only filesystem and no MCP/tool connection to the
  integration harness. It receives no database, NATS, wallet, exchange, or
  service credential and reviews session identity, replay, and model-controlled
  field injection only.
- No reviewer may repair the candidate. Any finding returns to a new bounded
  descendant task owned by the relevant component owner.

## Acceptance checks

- HEAD is the exact accepted P1-006 descendant; no merge commit or cherry-pick
  assembly exists in the P1 lineage.
- Source commits, patch IDs, component trees, and task-owned path sets match
  their accepted candidates.
- Contract, Go, race, PostgreSQL, NATS, migration, recovery, dependency, scope,
  secret, runtime-egress, authentication, and exposed-port checks pass.
- Every frozen failure point has deterministic expected evidence.
- Cross-owner mutation covers HTTP, established/new WebSocket, refresh,
  internal tool, repository, SQL, event, Inbox, Outbox, audit, and recovery.
- Device enrollment, fixation, full refresh-family revocation, independent
  throttle dimensions, request digest, AuditEvent linkage, service-principal
  subject permissions, and internal notifications pass their frozen probes.
- Duplicate requests and deliveries create one business effect and one required
  audit chain.
- Crash and restore preserve PostgreSQL facts and rebuild NATS from the complete
  Outbox without blind retry.
- Revoked sessions/devices and invalid/expired/consumed challenges fail within
  the frozen limits.
- Observed recovery measurements reproduce from the frozen workload and are not
  labeled as an SLA.
- Review findings are zero P0/P1/P2 with explicit verdict `PASS`.

## Hard boundaries

- `PASS` does not authorize merge, deployment, production, wallet access,
  exchange connectivity, exchange writes, automatic trading, or Phase 2.
- `MERGE/DEPLOY/PRODUCTION AUTHORIZED = NO`
