# P0-003 — Hermes Python and MCP contract conformance

## Identity

- Task ID: `P0-003`
- Owner: local Hermes development runtime
- Branch: `codex/p0-hermes-contracts`
- Base: exact accepted commit from `codex/p0-contracts-foundation`

## Objective

Implement the smallest `services/hermes-agent` Python module that validates
model-produced structured intents and exposes only the approved MCP tool
surface.

## Owned paths

- Allowed: `services/hermes-agent/**`
- Read-only: `contracts/**`, `docs/**`, root `AGENTS.md`
- Forbidden: `contracts/**`, `services/trading-core/**`, `infra/**`

## Acceptance checks

- Python tests pass in a project-local virtual environment.
- Valid shared fixtures validate and all shared negative fixtures fail closed.
- Unknown fields, float decimals, prompt-injected account IDs, and unsupported
  symbols/actions are rejected.
- MCP inventory contains no raw signing, raw order, withdrawal, transfer, SQL,
  or shell capability.
- The implementation consumes the frozen ten-tool inventory instead of
  defining an independent list.
- Session-owned `user_id` and `account_id` cannot be supplied by model output.

## Hard boundaries

- No exchange SDK write path, wallet, signer, deployment, or production model.
- No secrets in prompts, tests, logs, fixtures, or handoff.
- `MERGE/DEPLOY/PRODUCTION AUTHORIZED = NO`
