# P0-002 — Go contract and state-machine conformance

## Identity

- Task ID: `P0-002`
- Owner: server root Codex
- Branch: `codex/p0-go-contracts`
- Base: exact accepted commit from `codex/p0-contracts-foundation`

## Objective

Implement the smallest `services/trading-core` Go module that consumes the
frozen contracts, validates confirmation-bound inputs, and enforces Operation
and protection state transitions.

## Owned paths

- Allowed: `services/trading-core/**`
- Read-only: `contracts/**`, `docs/**`, root `AGENTS.md`
- Forbidden: `contracts/**`, `services/hermes-agent/**`, `infra/**`

## Acceptance checks

- `go test ./...` passes.
- Valid shared fixtures decode and validate.
- All shared negative fixtures fail closed.
- Every illegal state transition is rejected.
- Confirmation digest matches the shared golden vector and changes for every
  bound-field mutation.
- No floating-point type is used for price, amount, quantity, fee, or risk.

## Hard boundaries

- Workspace sandbox only; no sandbox bypass.
- No exchange, wallet, signer, network write, deployment, or production access.
- `MERGE/DEPLOY/PRODUCTION AUTHORIZED = NO`
