# P1-006 — Phase 1 integration, recovery, and security exit review

## Identity

- Task ID: `P1-006`
- Owner: local Codex coordinator with independent fixed-commit reviews
- Branch: `codex/p1-exit-review`
- Base: exact accepted commits from P1-001 through P1-005

## Objective

Prove Phase 1 interoperability, transaction atomicity, identity isolation,
idempotent delivery, crash recovery, secret hygiene, and default-disabled
infrastructure.

## Review roles

- Local Codex assembles exact accepted commits and owns the final evidence map.
- Server Codex runs Linux, PostgreSQL, NATS, process-kill, and recovery probes.
- Local Hermes performs read-only adversarial review of session-owned identity,
  model capability boundaries, replay, and prompt-controlled field injection.
- No reviewer may repair the candidate in the review worktree.

## Acceptance checks

- Exact source patch IDs and component trees match accepted task commits.
- All contract, Go, race, PostgreSQL, NATS, migration, recovery, dependency,
  scope, secret, and exposed-port checks pass.
- Every frozen failure-injection point has deterministic expected evidence.
- Cross-user/account mutation tests cover API, repository, direct SQL, event,
  Inbox, Outbox, audit, and streaming authorization paths.
- Duplicate requests and deliveries create exactly one business effect and one
  required audit chain.
- Crash and restore probes preserve authoritative PostgreSQL facts and rebuild
  derived delivery state without blind retry.
- Revoked sessions and expired/consumed device challenges fail across all
  ingress paths.
- Observed recovery measurements are reproducible and are not mislabeled as an
  SLA.
- Changed-path ownership and integration order match every task packet.
- Findings: zero P0/P1/P2; explicit verdict `PASS`.

## Hard boundaries

- Review `PASS` does not authorize deployment, production, wallet access,
  exchange connectivity, exchange writes, automatic trading, or Phase 2
  acceptance.
- `MERGE/DEPLOY/PRODUCTION AUTHORIZED = NO`
