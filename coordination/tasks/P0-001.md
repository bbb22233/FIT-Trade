# P0-001 — Shared contract foundation

## Identity

- Task ID: `P0-001`
- Owner: local Codex coordinator
- Branch: `codex/p0-contracts-foundation`
- Worktree: `/Users/guanlan/Documents/FIT-Trade-worktrees/p0-contracts`
- Base commit: `3d0d65d463fc329a68bbef3bed104e35dd92d37c`

## Objective

Freeze the cross-language Phase 0 semantics used by Go, Python, TypeScript, MCP,
REST, WebSocket events, confirmation hashing, and state-machine validation.

## Owned paths

- Allowed: `contracts/**`, `coordination/tasks/P0-*.md`
- Read-only dependencies: `docs/**`, root `AGENTS.md`
- Forbidden: `services/**`, `infra/**`, live exchange or wallet configuration

## Acceptance checks

- Canonical schemas accept all valid fixtures and reject every negative fixture.
- Every top-level domain schema has positive, missing-field, unknown-field,
  boundary, and malicious-input coverage.
- Decimal money, price, quantity, and risk values are strings, never JSON floats.
- Unknown fields are rejected in security-sensitive objects.
- Operation, protection, and automation state machines reject illegal moves.
- Every confirmation-bound field changes the deterministic SHA-256 digest.
- MCP surface contains no raw order, signing, transfer, withdrawal, SQL, or shell tool.
- MCP inventory contains exactly ten reviewed tools; user, account, and session
  scope never appears in model-controlled input schemas.
- JSON, OpenAPI, and Protobuf sources pass syntax/structure checks.
- OpenAPI external references resolve, Protobuf parses, and every top-level
  JSON domain object's field names match its Protobuf message.

## Hard boundaries

- No exchange write calls, signer access, deployment, or production activation.
- No credentials or real account identifiers in fixtures.
- `MERGE/DEPLOY/PRODUCTION AUTHORIZED = NO`
